# 福彩3D 杀和尾 — Hedge 加权投票机制说明（AI 可复现版）

> 版本：v3.2 固定专家模式 · 参数锁定 (win=40, K=650) · 2026-08-22 更新（账本机制已移除）
> 本文档面向 AI/开发者：按此规格可 1:1 复现系统，含伪代码与数据口径。

---

## 1. 任务定义

| 项 | 说明 |
|---|---|
| 游戏 | 福彩3D，每期开奖 3 个数字（百/十/个位，各 0-9） |
| 目标 | 预测**下期和尾** `(b+s+g) % 10` 不会出现的数字，杀掉 1 个 |
| 命中判定 | 实际和尾 ≠ 杀码 → 命中 |
| 随机基线 | 杀掉 1 个数字，90% 概率天然命中（10 个尾杀 1 个） |
| 硬约束 | 严格 walk-forward：预测第 t 期只用 ≤ t-1 期数据，绝不偷看未来 |

---

## 2. 数据

- 来源：`fc3d-history.csv`（2002001 ~ 2026223，8731 期）
- 列：`issue, hundreds, tens, ones`
- 云端每晚 21:30 开奖后自动追加最新一期

---

## 3. 特征体系（96 特征）

期 t 的特征向量由**期 t-1、期 t-2** 计算（`formulas.feat_list`）：

```
feat_t = feat_list(h[t-1], t[t-1], o[t-1], prev=(h[t-2], t[t-2], o[t-2]))
```

类型包括：历史频率、差值、振幅、冷热号、跨位组合等（FEAT_VERSION 版本化管理）。
**关键**：特征只用过去 2 期数据 → 严格不偷看未来。

---

## 4. 专家池构建（永久固定，一次性完成）

```
候选空间：39,311,040 个公式（96 特征线性组合，含单/多特征 + 常数项）
每个公式（专家）形态：
    pred = (Σ coeff_i × feat_i + const) mod 10     # 输出 0-9 = 该专家"杀"的数字
    terms = [(coeff, feat_index), ...], const ∈ 整数

挑选规则：
    1. 在最新 500 期（2025075~2026223）上逐期计算每个公式的杀中率
    2. 按杀中率降序取 Top 800
    3. 族约束：公式按特征组合分成 7 族（A/AB/AC/ABC/B/BC/C），
       每族限选 ~115 条，防止 800 专家同质化
锁定：挑选完成后【永久固定】，不再随数据滑动重选
```

---

## 5. Hedge 加权投票（每期执行的核心算法）

```
输入：
    pool      = 800 个固定专家
    win       = 40    # 权重窗口（近 40 期命中率）
    K         = 650   # 每期实际参与投票的专家数
    SMOOTH    = 0.02  # 权重下限
    pred[i,j] = 专家 i 对列 j（数据期）输出的杀数
    hit[i,j]  = 专家 i 在列 j 是否杀中（pred != 实际和尾）

对第 t 期（列 j）预测：
    step 1  对每个专家 i：rate_i = mean(hit[i, j-win : j])   # 近 40 期命中率
    step 2  权重 w_i = max(rate_i, SMOOTH)                    # 命中率即权重，下限 0.02
    step 3  按 w_i 降序取前 K=650 名专家                       # 每期动态选 Top K
    step 4  650 名专家各报出自己对该期的杀数 = pred[i, j]
    step 5  数字 c 的得票 = Σ { w_i : pred[i,j] == c }        # ★ 加权合计，非人头数
    step 6  票王 kill = argmax(得票)  → 本期杀和尾
    step 7  Top3 票码 = 得票前三名（票王 + 第2 + 第3）

伪代码（numpy 风格）：
    rates = hit[:, j-win:j].mean(axis=1)          # (800,)
    ti    = np.argsort(-rates)[:K]                 # 前 650 索引
    w     = np.maximum(rates[ti], SMOOTH)          # (650,)
    votes = np.bincount(pred[ti, j], weights=w, minlength=10)
    kill  = int(np.argmax(votes))
```

**"得票"含义**：不是"几个专家投了 3"，而是"投 3 的专家们近 40 期命中率权重之和"——近期更准的专家话语权更大（这就是 Hedge 对冲加权）。

---

## 6. 参数锁定与确定性（v3.2 核心）

```
pool.json 写入 locked = {"win": 40, "k": 650} 后：
  ① 专家池永久固定（不再穷举重选）
  ② win/K 参数固定（不再每日网格扫描）

→ 第 i 期杀码 = 纯确定性函数 f(≤i-1期数据, 固定池, 固定参数)
→ 无论何时重算，同一期结果永远一致
→ 发布值 = 回测值，可随时对账
```

云端流水线（`update_v3.py`，每晚开奖后自动）：
```
[1/5] 联网补抓最新开奖（多源降级 + CSV 兜底）
[2/5] 专家池已锁定 → 跳过 3931 万穷举（省 ~6 分钟）
[3/5] 参数已锁定 → 跳过网格扫描
[4/5] 500 期逐期回测 + 下期预测（walk-forward）
[5/5] 生成静态页面 index.html
总耗时 ≈ 24 秒（锁定前 ≈ 7-8 分钟）
```
> 注：v3.2 曾含 [4.5] 发布账本（开奖前发布记录 + 开奖后补标），2026-08-22 已按需求移除（ledger.py / cache/predictions.json 删除）。回测口径 = 确定性算法回测。

---

## 7. K=650 的选优依据（四窗口网格扫描）

| 窗口 | 角色 | 结论 |
|---|---|---|
| 近 100 期 | 选优口径1 | K≥150 全部 100%（饱和） |
| 近 200 期 | 选优口径2 | K≥150 全部 100%（饱和） |
| 近 500 期 | 选优口径3 | K≥200 全部 100%（饱和）⚠️ 此段 = 专家挑选窗，选择偏差 |
| **近 1000 期** | **破平局** | **超挑选窗 = 真实区分段**：K=650 → **96.40% 全场最高** |
| 样本外 2000 期 | 参考 | 91.35% |
| 验证段 500 期 | 参考 | 91.8% |

选优规则：`近100 → 近200 → 近500 → 近1000 → K大`。前三窗全并列 100% 时，由近 1000 期破平局 → **K=650 胜出**。

---

## 8. 性能口径（诚实分层，严禁混用）

| 口径 | 数值 | 性质 |
|---|---|---|
| 训练窗 500 期 | 100% | ⚠️ 选择偏差（专家正是这段选出的，"先看答案再选人"），**不代表未来** |
| 近 1000 期 | 96.4% | 超出挑选窗，真实段 |
| 样本外 2000 期（2019144~2025074） | 91.35% | 专家从未见过 → **真实未来预期** |
| 验证段 500 期（2018002~2019143） | 91.8% | 更早段，稳健性检验 |
| 随机基线 | 90% | 数学下限 |

**最终结论**：真实水平 ≈ 91%（仅超基线 ~1pp），91% 是数学天花板（每尾必现约 10%），**不存在稳定超额**，不构成购彩建议。

---

## 9. 项目文件地图

| 文件 | 职责 |
|---|---|
| `formulas.py` | 96 特征定义（feat_list） |
| `bruteforce500.py` | 3931 万公式穷举选专家池 |
| `hedge_core.py` | 矩阵构建 + hedge_vote + 回测 + 下期预测（主引擎） |
| `grid_scan_k4.py` | 四窗口 K 网格扫描自动选优 |
| `update_v3.py` | 云端一键更新流水线（每晚自动） |
| `gen_site.py` | 生成静态页面 index.html |
| `cache/pool.json` | 固定专家池 + 锁定参数 |
| `cache/result.json` | 回测与下期预测结果 |

---

## 10. 一句话总结（给 AI 的 TL;DR）

> 这是一个"固定 800 公式专家池（由 3931 万公式在近 500 期按命中率选出、永久锁定）+ 每期按近 40 期命中率加权取前 650 名投票、票王杀和尾"的确定性系统。训练段 100% 是过拟合假象，样本外真实预期 91% ≈ 基线 + 1pp，数学上无稳定超额。
